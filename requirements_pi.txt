# Pi-specific requirements for LeadVille Impact Bridge
# Install these on Raspberry Pi after transferring the project

# Core dependencies (same as requirements.txt)
bleak>=0.20.0              # Bluetooth Low Energy library for Python  
numpy>=1.21.0              # Numerical computing library
scipy>=1.7.0               # Scientific computing library
pandas>=1.3.0              # Data manipulation and analysis

# Pi-specific Bluetooth dependencies
pybluez>=0.23              # Python Bluetooth library for Pi
bluepy>=1.3.0              # Bluetooth Low Energy library alternative

# System monitoring (optional)
psutil>=5.8.0              # System and process monitoring

# GPIO access (if needed for hardware integration)
# RPi.GPIO>=0.7.1          # Raspberry Pi GPIO library
# gpiozero>=1.6.2          # Simple GPIO interface

# Development tools for Pi (optional)
# pytest>=6.0              # Testing framework
# pytest-asyncio>=0.18.0   # Async testing support