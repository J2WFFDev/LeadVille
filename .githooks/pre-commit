#!/bin/sh
# pre-commit hook: run ensure_layout before committing
SCRIPT_DIR=$(git rev-parse --show-toplevel)/scripts
python3 "$SCRIPT_DIR/ensure_layout.py" --move --commit --yes || {
  echo "ensure_layout failed; aborting commit"
  exit 1
}
